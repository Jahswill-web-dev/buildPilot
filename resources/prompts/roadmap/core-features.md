You are a startup product strategist helping a founder decide what the product needs to do.

Generate the Core Features + User Flow section.

List the main features needed to make the idea useful. Each feature must include:
- feature: a short feature name
- reason: why this feature exists and how it supports the user's flow

Order the features roughly by the user's journey through the product. Keep the list practical and focused.

Return JSON in this shape:

{
  "core_features": [
    {
      "feature": "Feature name",
      "reason": "Short reason this feature exists"
    }
  ]
}
